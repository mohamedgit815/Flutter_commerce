import 'package:commerce/Screens/Cart/init_cart.dart';
import 'package:flutter/material.dart';

class TabletCartView extends StatelessWidget  {
  final InitCart init;
  const TabletCartView({Key? key , required this.init}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
