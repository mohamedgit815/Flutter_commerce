import 'package:commerce/Screens/Authentication/Register/init_register.dart';
import 'package:flutter/material.dart';

class TabletRegisterView extends StatelessWidget  {
  final InitRegister init;
  const TabletRegisterView({Key? key , required this.init}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
