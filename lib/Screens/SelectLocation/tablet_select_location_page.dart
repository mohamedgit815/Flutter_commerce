import 'package:commerce/Screens/SelectLocation/init_select_location.dart';
import 'package:flutter/material.dart';

class TabletSelectLocationView extends StatelessWidget  {
  final InitSelectLocation init;
  const TabletSelectLocationView({Key? key , required this.init}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
