import 'package:commerce/Screens/Orders/init_orders.dart';
import 'package:flutter/material.dart';

class TabletOrdersView extends StatelessWidget  {
  final InitOrders init;
  const TabletOrdersView({Key? key , required this.init}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
