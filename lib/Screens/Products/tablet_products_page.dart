import 'package:commerce/Screens/Products/init_products.dart';
import 'package:flutter/material.dart';

class TabletProductsView extends StatelessWidget  {
  final InitProducts init;
  const TabletProductsView({Key? key , required this.init}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
